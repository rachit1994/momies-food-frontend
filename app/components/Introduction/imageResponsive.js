import React from 'react';
import styled from 'styled-components';
import Grid from '@material-ui/core/Grid';

export const ImageResponsive = styled(Grid)`
    img {
        width: 150px;
        height: 120px;
    }
`;

export default ImageResponsive;
